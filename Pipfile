[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
tsapython = ">=2.0.0"
pyserial = ">=3.5"
numpy = ">=1.21.0"
pandas = ">=1.3.0"
pyyaml = ">=6.0"
matplotlib = ">=3.5.0"

[dev-packages]

[requires]
python_version = "3.11"
